workspace(name = "com_chokobole_bazel_utils")

load("//:buildifier_deps.bzl", "buildifier_deps")

buildifier_deps()

load("//:buildifier_deps_deps.bzl", "buildifier_deps_deps")

buildifier_deps_deps()
